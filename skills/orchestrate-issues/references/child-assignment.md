# Child Assignment

Fill in the placeholders and send after model/effort metadata has been verified. Include the parent's actual authorization record; do not manufacture approval.

> Campaign: <campaign ID>. Issue: <repository and issue URL>. Lineage: <initial issue or originating issue and accepted recommendation>. Dependencies: <merged prerequisite URLs or none>.
>
> Your session has been verified as <exact model> with effort <verified effort or not applicable>, on <harness>. Selection policy: <which dimensions Wollipog campaign policy fixed and which the parent chose automatically, with rationale>. Do not change your session configuration yourself; report a capability limitation or need for reconsideration to the parent. Claim and deliver this issue following using-wollipog, the repository instructions, and the repository's issue workflow and review requirements. Recheck claims and overlapping work before starting. Use the isolated Wollipog worktree already attached to this session if appropriate; verify its branch and base rather than creating a redundant worktree. Report a contested claim before implementing.
>
> Deliver all acceptance criteria, using multiple PRs if needed. Complete tests, review, UI evidence where applicable, required CI, actual merge verification, tracker updates, and authorized branch cleanup. An enqueued PR is unfinished. Preserve stacked dependents and use Wollipog for worktree retirement. Report active-worktree cleanup deferral so the parent can finish it after your provider exits.
>
> Do not update, stop, or restart the Wollipog installation (control plane, runner, or development stack) that hosts this session, including through a detached updater or supervisor. After merge, report the exact merge commit and deployed commit if known. Deployment is a handoff to a human terminal or an operator proven to be outside the target stack; do not make it live yourself and do not treat that handoff as unfinished issue implementation.
>
> Authorization and outstanding gates: <exact delegated decisions, approved payloads or PR snapshots, and decisions still requiring the user>. Follow current policy. Ask each unresolved decision once with concrete review material using the supported structured mechanism. Parent prompts do not replace required approval-tool responses.
>
> Stay on this issue. Do not select unrelated work, publish follow-ups, or start follow-up implementation on your own. The parent evaluates and deduplicates proposals, coordinates required publication approval, and assigns accepted issues. Any review the repository requires remains part of your assignment.
>
> Implement assigned work in this session. Receiving multiple issue numbers does not authorize spawning implementation children or invoking orchestrate-issues; further delegation requires an explicit assignment authorizing it.
>
> A separate parent assignment may ask you to publish a follow-up draft you own, carrying the exact approved payload and authorization evidence. Verify that authorization under current policy, recheck duplicates, publish and read it back, then report the URL. This does not authorize other publications or implementation before assignment.
>
> Report issue and PR URLs, delivered/remaining acceptance criteria, reviewed head and merge commit, test/check evidence, branch/worktree cleanup state, blockers, and residual risks. End with Recommended Follow-Ups: for each, give a proposed title, concrete evidence, rationale, bounded scope, acceptance criteria, and known duplicates. Separate borderline observations that do not merit an issue. Say “No follow-up issues recommended” when none qualify. Wait for a parent assignment after reporting; do not archive yourself.
